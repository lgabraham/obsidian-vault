# F&S
Resolve multiple product URLs in sitemap
Make sure the collection pages are using custom title tags specific to the collection, as written by our writers.
Implement the Footer on Mobile View
Introduce a secondary collection page navigation option to the site (other than the filter menu)
Make sure the **sitemap**page in the footer is server-side rendered, not client-side